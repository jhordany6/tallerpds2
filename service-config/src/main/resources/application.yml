spring:
  application:
    name: service-config
  cloud:
    config:
      server:
        git:
          uri: file:///E:/Universidad/PDS/config-data
  security:
    user:
      name: admin
      password: admin
server:
  port: 9000