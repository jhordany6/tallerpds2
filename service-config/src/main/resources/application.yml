spring:
  application:
    name: service-config
  cloud:
    config:
      server:
        git:
          uri: file:///E:/Universidad\PDS\Taller_2\remote-server-properties
  security:
    user:
      name: admin
      password: admin
server:
  port: 9000